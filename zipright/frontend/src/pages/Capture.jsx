import PoseGuide from "../components/PoseGuide";

export default function Capture() {
  return (
    <section>
      <h1>Capture</h1>
      <p>Upload one front-facing photo and one side-facing photo, then validate the pose before measuring.</p>
      <PoseGuide />
    </section>
  );
}
